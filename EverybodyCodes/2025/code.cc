#include "../utils.cc"

void solve() {
    ll res = 0;
    vector<ll> inp;
    vector<string> ss = getInp([&](auto& in, int i) {
        if(!i) {

        } else if(i > 0) {
            
        }
        //ll _k; cin >> _k; inp.pb(_k);
        /*
        ll k; char c;
        do {
            cin >> k;
            inp.pb(k);
        } while(cin >> c);
        */
    });

    cout << res << endl;
}

